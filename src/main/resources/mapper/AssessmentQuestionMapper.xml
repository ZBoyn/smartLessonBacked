<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neu.smartLesson.mapper.AssessmentQuestionMapper">

    <insert id="insertBatchQuestions">
        INSERT INTO AssessmentQuestions (assessment_id, question_id, points, order_index)
        VALUES
        <foreach collection="questionIds" item="qId" index="index" separator=",">
            (#{assessmentId}, #{qId}, 10, #{index})
        </foreach>
    </insert>

    <select id="findQuestionIdsByAssessmentId" parameterType="int" resultType="int">
        SELECT question_id FROM AssessmentQuestions
        WHERE assessment_id = #{assessmentId}
        ORDER BY order_index
    </select>
</mapper>