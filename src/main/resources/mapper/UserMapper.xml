<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.neu.smartLesson.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.neu.smartLesson.model.User">
        <id property="userId" column="user_id" />
        <result property="role" column="role" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM Users WHERE username = #{username}
    </select>

    <select id="findByEmail" parameterType="string" resultMap="UserResultMap">
        SELECT * FROM Users WHERE email = #{email}
    </select>

    <insert id="insertUser" parameterType="com.neu.smartLesson.model.User"
            useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO Users
            (username, password_hash, full_name, email, role)
        VALUES
            (#{username}, #{passwordHash}, #{fullName}, #{email}, #{role, typeHandler=org.apache.ibatis.type.EnumTypeHandler})
    </insert>

</mapper>